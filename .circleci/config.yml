# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      # use `-browsers` prefix for selenium tests, e.g. `3.6.1-browsers`
      - image: circleci/python:2.7.14

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    working_directory: ~/repo

    environment:
      SQLALCHEMY_DATABASE_URI_PROD: 'mysql://user:password@localhost/notejam'

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "flask/requirements.txt" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run:
          name: install dependencies
          command: |
            sudo pip install --upgrade pip setuptools
            sudo pip install -r flask/requirements.txt

      - save_cache:
          paths:
            - ./venv
          key: v1-dependencies-{{ checksum "flask/requirements.txt" }}

      # run tests!
      - run:
          name: run tests
          command: |
            python flask/tests.py

      - run:
          name: generate deployment artifacts
          command: |
            sed -i "s%__SQLALCHEMY_DATABASE_URI_CI_CHANGE_VAR__%$SQLALCHEMY_DATABASE_URI_PROD%" ./flask/notejam/config.py
            tar czfv notejam-flask.tgz --exclude='*.pyc' --exclude='*.db' --recursion flask


      - store_artifacts:
          path: notejam-flask.tgz
          destination: notejam-flask.tgz

  deploy-prod:

    machine:
      enabled: true
    #environment:
    #  SQLALCHEMY_DATABASE_URI_PROD = $SQLALCHEMY_DATABASE_URI_PROD

    working_directory: ˜/repo

    steps:

      - run:
          name: deploy package to S3
          command: aws s3 cp notejam-flask.tgz s3://frmaia-notejam/notejam-flask-latest.tgz


workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy-prod:
          requires:
            - build
          filters:
            branches:
              only: master
